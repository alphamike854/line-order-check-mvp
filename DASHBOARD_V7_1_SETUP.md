# Dashboard v7.1 — รูด / บล / บลก / ไม่เอาเบิ้ล

รอบนี้เพิ่มภาษาที่ใช้จริงในกลุ่ม LINE ให้ Parser โดยตรง

## คำย่อที่รองรับ

- `บล` = A+B
- `ก` = กลับรหัส
- `บลก` = A+B+กลับรหัส
- `รูด` = ชุดเลขหลักสิบ เช่น `รูด 1` คือ 10–19
- `รูดเบิ้ล` = 00,11,22,...99
- `(ไม่เอาเบิ้ล)` หรือ `ไม่เบิ้ล` = ตัดเลข 00,11,...99 ออกจากผลลัพธ์หลังสร้างชุดและกลับรหัส

## ตัวอย่าง

`รูด 1-300 บล`

- A10–A19 = 300
- B10–B19 = 300
- รวม 6,000

`รูด 1-300 บลก`

- สร้าง 10–19 และเลขกลับ
- 11 นับครั้งเดียว
- ลงทั้ง A และ B
- รวม 11,400

`รูด 0-500 บลก (ไม่เอาเบิ้ล)`

- ได้ 01–09 และ 10,20,...90
- ไม่เอา 00
- ลงทั้ง A และ B
- รวม 18,000

`รูดเบิ้ล 300 บล`

- 00,11,...99
- ลงทั้ง A และ B
- รวม 6,000

`08 09 / 45 65 / =250 บลก` รองรับ `บลก` เป็นคำสั่งรวม A+B+กลับรหัสด้วย

## Alias Settings

เพิ่ม target:

- `AB — A+B`
- `ABC — A+B+กลับ`

Migration 015 จะเพิ่มคำมาตรฐาน `บล`, `ก`, `บลก`, `รูด`, `รูดเบิ้ล` ถ้ายังไม่มี โดยไม่เขียนทับ Alias ที่มีอยู่แล้ว

## ติดตั้ง

1. แตก patch v7.1 ทับ v7.0
2. Run `supabase/migrations/202608260015_add_composite_aliases_and_sweep_shorthand.sql` ใน Supabase SQL Editor
3. Run `npm test`
4. ตรวจ `node --check src/lib/order-parser.mjs public/app.js`
5. `git add .` → `git diff --cached --check` → commit → push
